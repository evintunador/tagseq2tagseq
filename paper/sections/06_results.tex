\section{Results}
\label{sec:results}

All numbers in this section are generated from \texttt{provenance/ledger.yaml} (via
\texttt{scripts/gen\_values\_tex.py}) and cited with generated \verb|\val| macros, so each
traces to the run and code state that produced it; see \Cref{app:recipe} and
\texttt{provenance/README.md}.

\subsection{Cross-document attention beats flat concatenation}
On the Wikipedia HotpotQA cross-document benchmark, the link-aware \crossdoc model attains
a cross-document negative log-likelihood of \val{crossdoc.wiki_hotpotqa.nll_crossdoc}
against \val{crossdoc.wiki_hotpotqa.nll_flat} for the flat-linked baseline over the same
$n=\val{crossdoc.wiki_hotpotqa.n}$ query--target pairs, a reduction of
$\Delta\mathrm{nll}=\val{crossdoc.wiki_hotpotqa.delta_nll}$. The effect carries over to code:
on Java RepoBench cross-document completion, cross-document attention lowers NLL by
\val{crossdoc.repobench_java.bfs.delta_nll} (bfs), \val{crossdoc.repobench_java.dfs.delta_nll}
(dfs), and \val{crossdoc.repobench_java.rw.delta_nll} (random-walk traversal).
\todo{Python RepoBench cross-doc (reported +0.09 at max\_grants=256) is not yet grounded:
the corrected re-eval was written to a sidecar JSON that the distiller does not ingest, so
the distilled record still holds the superseded 64-cap value. Re-eval into eval\_results.json,
re-distill, then add the ledger entry.}

\subsection{Single-document benchmarks are unaffected}
\todo{All four masks statistically indistinguishable on hellaswag/arc/piqa/etc ---
cross-doc attention has nothing to attend to on isolated docs, as predicted. Ground once
the per-benchmark accuracies + CIs are selected.}

\subsection{Compute controls}
Granting the baselines more within-context attention does not close the gap. On code
RepoBench perplexity, \crossdoc reaches \val{compute.repobench_ppl.cross_doc_link}, whereas
\concatlink and \concat --- which see the same concatenated multi-document context --- sit at
\val{compute.repobench_ppl.doc_concat_link} and \val{compute.repobench_ppl.doc_concatenated}
respectively, and the plain \doccausal baseline at \val{compute.repobench_ppl.doc_causal}.
The compute controls remain well above \crossdoc, so the gain comes from the linking
inductive bias, not from additional attention FLOPs.

\subsection{Traversal ablation}
Graph-structured packing matters for the base language model independent of the
cross-document objective: final validation loss is \val{traversal.wiki_val_loss.graph}
under graph (bfs/dfs) packing versus \val{traversal.wiki_val_loss.random} under random
packing. The incremental cross-document benefit is itself traversal-robust --- the Java
cross-document $\Delta$nll above is positive and similar across bfs, dfs, and random-walk
traversals --- so packing quality and cross-document attention are two decoupled axes.
(The two validation-loss figures are read from training logs, not the eval harness, and are
tracked as grounding debt until a log-value source lands; see \texttt{provenance/README.md}.)

\subsection{Diversity scaling (merged multi-source model)}
\todo{11-source merge's cross-doc $\Delta$ is 1.7--11$\times$ larger than
single-domain specialists despite far less per-domain data; flat 3.9B$\to$8B; joint
training over many link types strengthens the cross-doc machinery itself. Aggregate over
per-port harness JSONs (results/merged\_v2\_8b\_ports\_useline/); not yet an eval-grounded
ledger entry.}

\subsection{Density-aware scheduling efficiency}
\todo{1.45$\times$ mean / 1.14$\times$ max step speedup; CoV collapse within buckets.
Wall-clock timing from step-timing logs, not the eval harness --- needs a log-value source.}
