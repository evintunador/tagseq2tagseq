# A1 IO-aware/programmable attention (agent ac80dcdb) — 12 verified. (FA1/2, FlexAttn already round1.) OVERLAPS A6/A2/R3 — dedup on merge.
Keys: shah2024flashattention3(NeurIPS24 2407.08608 dup A6), rabe2021selfattention(2112.05682 dup A6, O(n²)-memory origin), pagliardini2023sparseflash(2306.01160 — CLOSEST precedent: flash kernel consuming RUNTIME sparse block pattern, but content/hash-driven vs our graph-link), wang2024flashmask(2410.01359 — column-wise sparse mask, competitor to FlexAttn mask_mod; but contiguous intervals can't express our arbitrary A→B rectangles → motivates bit-packed grants), milakov2018onlinesoftmax(dup A6), tillet2019triton(dup A6), gray2017blocksparse(dup A6/A2), lefaudeux2022xformers(mem-efficient attn lib), tay2020efficienttransformers(survey dup A2), yuan2025nativesparse(2502.11089 dup A2), lu2025moba(2502.13189 dup A2), ye2025flashinfer(2501.01005 dup R3).
NOTES: pagliardini + wang2024flashmask = strongest "existing mask-aware flash doesn't suffice for arbitrary graph grants" citations. Heavy dedup needed (shah/rabe/milakov/tillet/gray→A6; yuan/lu/tay→A2; flashinfer→R3).
