import { VALUE } from "../consts";       // relative up-dir -> src/consts
const lodash = require("lodash");        // bare CommonJS require -> external, NO edge

export function helper(model: unknown, react: unknown, cfg: unknown): number {
  return VALUE;
}
